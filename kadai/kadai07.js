function area(p1, p2) {
    let p = p1 * p2 / 2;        
    return p;               
}

console.log(area(10, 20));


var area = function(p1, p2) {
    let p = p1 * p2 / 2;       
    return p;               
}

console.log(area(10, 10));